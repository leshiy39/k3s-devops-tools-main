apiVersion: v1
kind: Service
metadata:
  name: node-exporter
spec:
  type: {{ .Values.service.type }}
  selector:
    app: node-exporter
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9100
