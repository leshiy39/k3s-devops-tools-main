replicaCount: 1
nameSpace: kube-system

image:
  repository: kubernetesui/dashboard
  tag: v2.7.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9090

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: dashboard.devops-tools-dev.ps.ad.speechpro.com
      paths:
        - path: /
          pathType: Prefix
