apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: portainer-binding
subjects:
  - kind: ServiceAccount
    name: portainer-sa
    namespace: default
roleRef:
  kind: ClusterRole
  name: portainer-role
  apiGroup: rbac.authorization.k8s.io
